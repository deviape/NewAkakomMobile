<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.antarmukamobile.akakommobile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.Feedback" />
        <activity android:name=".ui.BeritaActivity" />
        <activity android:name=".ui.Tautan" />
        <activity android:name=".ui.TautanWebView" />
        <activity android:name=".ui.InfoActivity" />
        <activity
            android:name=".ui.DetailBeritaActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.Prodi"
            android:theme="@style/AppTheme.NoActionBar"  />
        <activity android:name=".ui.ProfileVisi"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>